# kubernetes-models-ts

## Install

```sh
npm install kubernetes-models
```

## Usage

```js
import { Pod } from "kubernetes-models/api/core/v1";

const pod = new Pod({
  //
});
```

## Build

```sh
make dist
```
