---
"@kubernetes-models/gateway-api": major
---

Switch to go-generate. Types now sourced from upstream Go packages — gateway-api v1.5.1.

Field types are now easier to reference: instead of `IHTTPRoute['spec']`, use `IHTTPRouteSpec`.
