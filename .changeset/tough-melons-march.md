---
"kubernetes-models": patch
---

`apiVersion` and `kind` are no longer required in `volumeClaimTemplates` property of `StatefulSet` resource. (Fix [#44](https://github.com/tommy351/kubernetes-models-ts/issues/44))
